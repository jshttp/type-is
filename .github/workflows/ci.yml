name: CI
on:
  - push
  - pull_request
permissions:
  contents: read
jobs:
  test:
    name: Test - ${{ matrix.os }} / Node.js ${{ matrix.node-version }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        node-version:
          - 18
          - '*'
        # The ts-scripts format step is known to trip on Windows line endings;
        # keep the Windows legs informational until that is measured in CI.
        include:
          - os: windows-latest
            experimental: true
    continue-on-error: ${{ matrix.experimental == true }}
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node-version }}
      - run: npm install
      - run: npm test
      - uses: codecov/codecov-action@v5
        with:
          name: ${{ matrix.os }} / Node.js ${{ matrix.node-version }}
          token: ${{ secrets.CODECOV_TOKEN }}
